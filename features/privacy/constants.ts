/** In-app legal content — shown on Settings screens, not external URLs. */
export const DATA_PROMISE =
  'Your health data is yours. We collect only what a feature needs, never sell it, encrypt it in transit and at rest, and let you export or delete everything at any time.';
